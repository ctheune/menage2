###
# app configuration
# https://docs.pylonsproject.org/projects/pyramid/en/latest/narr/environment.html
###

[app:main]
use = egg:menage2

pyramid.reload_templates = false
pyramid.debug_authorization = false
pyramid.debug_notfound = false
pyramid.debug_routematch = false
pyramid.default_locale_name = de
sqlalchemy.url = sqlite:///%(here)s/menage2.sqlite

menage.timezone = Europe/Berlin

retry.attempts = 3

# Sessions (beaker + Redis)
# Set session.url to redis://:PASSWORD@localhost:6379/0 if using a Redis password
session.type = ext:redis
session.url = redis://localhost:6379/0
session.key = menage_session
# generate with: openssl rand -hex 32
session.secret = CHANGE_THIS_TO_A_LONG_RANDOM_SECRET
session.timeout = 259200
session.cookie_expires = 259200
session.httponly = true
session.secure = true
session.samesite = Lax

# SMTP for password recovery
mail.host = localhost
mail.port = 587
mail.username =
mail.password =
mail.from_address = menage@localhost
mail.use_tls = true

[pshell]
setup = menage2.pshell.setup

###
# wsgi server configuration
###

[alembic]
# path to migration scripts
script_location = src/menage2/alembic
file_template = %%(year)d%%(month).2d%%(day).2d_%%(rev)s
# file_template = %%(rev)s_%%(slug)s

[server:main]
use = egg:waitress#main
listen = *:6543
url_scheme = https

###
# logging configuration
# https://docs.pylonsproject.org/projects/pyramid/en/latest/narr/logging.html
###

[loggers]
keys = root, menage2, sqlalchemy, alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console

[logger_menage2]
level = WARN
handlers =
qualname = menage2

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine
# "level = INFO" logs SQL queries.
# "level = DEBUG" logs SQL queries and results.
# "level = WARN" logs neither.  (Recommended for production systems.)

[logger_alembic]
level = WARN
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(asctime)s %(levelname)-5.5s [%(name)s:%(lineno)s][%(threadName)s] %(message)s
